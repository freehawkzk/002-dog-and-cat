请帮我编写一个完整可运行的Python脚本，用于实现猫狗图像二分类任务，严格按照以下所有要求实现：

一、数据集配置
1. 数据集路径：项目根目录下的 data 文件夹
2. 数据集格式：标准分类文件夹结构（data/cat、data/dog）
3. 自动划分训练集、验证集，无需额外配置

二、模型结构要求
1. 构建3层CNN卷积神经网络
2. 每一层卷积后使用ReLU激活函数
3. 搭配池化层、全连接层，最后输出2分类结果
4. 框架使用PyTorch

三、训练参数要求
1. 损失函数：使用交叉熵损失
2. 每训练5个epoch，执行一次验证集评估
3. 训练过程必须记录：
   - 训练集损失
   - 训练集精度
   - 验证集精度

四、绘图要求
1. 训练结束后，在**同一张图**中绘制三条曲线：
   - 训练损失曲线
   - 训练精度曲线
   - 验证精度曲线
2. 包含图例、坐标轴标签、标题，清晰可读

五、模型保存要求
1. 保存两个模型文件：
   - best_model：测试集精度最高的模型
   - latest_model：最后一个epoch的最新模型
2. 加载精度最高的best_model，将其**转换为ONNX格式**并保存

六、环境说明
1. 使用Python虚拟环境，环境名：zk
2. 代码中包含所有必需依赖库的导入语句
3. 代码可直接运行，无语法错误，注释清晰

七、代码规范
1. 结构完整：数据加载 → 模型定义 → 训练循环 → 验证 → 绘图 → 保存模型 → 转ONNX
2. 适配新手，逻辑简单易懂
3. 无冗余代码，可直接在你的环境中运行